// The blog is disabled (`apps.blog.isEnabled: false` in src/config.yaml) and the
// demo posts were deleted, so there are no content collections. Astro still
// expects this file to exist and export `collections`.
export const collections = {};
